#load "ast.cmo";;

open Ast

module Tmap = Map.Make(String);;

exception TypeError of position



let type_decl decl m = match decl with
  |Decl_type i -> Tmap.add (i,Type(Void))
  |Decl_access i j -> let t = try Tmap.find j m with
			      |Not_found -> raise 
